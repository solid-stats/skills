export const meta = {
  name: 'audit-v1.1-spotcheck',
  description: 'Adversarially verify a stratified sample of the v1.1 replays-fetcher audit findings against the real source to measure a false-positive rate on the saved milestone artifact.',
  phases: [{ title: 'Verify' }],
}

const REPO = '/home/afgan0r/Projects/SolidGames/replays-fetcher'
const SAMPLE = [{"i": 0, "ruleId": "lsp-no-as-contract-defeat", "severity": "🔴", "file": "src/config.ts", "lineStart": 198, "lineEnd": 198, "quote": "  return value as SourceTransport;", "citation": "std: correctness → Contract compliance (LSP)", "kind": "per-file", "mechanical": false}, {"i": 1, "ruleId": "lsp-no-as-contract-defeat", "severity": "🔴", "file": "src/discovery/discover.ts", "lineStart": 128, "lineEnd": 128, "quote": "    const parsed = JSON.parse(text) as Partial<SourceFixture>;", "citation": "std: correctness → Contract compliance (LSP)", "kind": "per-file", "mechanical": false}, {"i": 2, "ruleId": "lsp-no-as-contract-defeat", "severity": "🔴", "file": "src/run/golden-fixtures.ts", "lineStart": 59, "lineEnd": 61, "quote": "const manifest = JSON.parse(\n    readFileSync(manifestPath, \"utf8\"),\n  ) as GoldenManifest;", "citation": "std: correctness → Contract compliance (LSP)", "kind": "per-file", "mechanical": false}, {"i": 3, "ruleId": "lsp-no-as-contract-defeat", "severity": "🔴", "file": "src/staging/payload.ts", "lineStart": 63, "lineEnd": 65, "quote": "const { day, hour, minute, month, second, year } = match.groups as Record<\n    \"day\" | \"hour\" | \"minute\" | \"month\" | \"second\" | \"year\",\n    string\n  >;", "citation": "std: correctness → Contract compliance (LSP)", "kind": "per-file", "mechanical": false}, {"i": 4, "ruleId": "no-blocking-io-async", "severity": "🔴", "file": "src/storage/store-raw-replay.ts", "lineStart": 40, "lineEnd": 40, "quote": "    const checksum = calculateSha256(bytes);", "citation": "std: correctness → Async safety — no blocking I/O", "kind": "per-file", "mechanical": false}, {"i": 5, "ruleId": "band-no-skip", "severity": "🟠", "file": "src/commands/shared.ts", "lineStart": 13, "lineEnd": 13, "quote": "import { discoverReplaysDryRun } from \"../discovery/discover.js\";", "citation": "conv: §A — Boundary fences fence #2", "kind": "per-file", "mechanical": false}, {"i": 6, "ruleId": "band-no-skip", "severity": "🟠", "file": "src/commands/watch.ts", "lineStart": 11, "lineEnd": 11, "quote": "import type { StagingRepository } from \"../staging/stage-raw-replay.js\";", "citation": "conv: §A — Boundary fences fence #2", "kind": "per-file", "mechanical": false}, {"i": 7, "ruleId": "band-no-skip", "severity": "🟠", "file": "src/commands/run-once.ts", "lineStart": 16, "lineEnd": 16, "quote": "import type { StagingRepository } from \"../staging/stage-raw-replay.js\";", "citation": "conv: §A — Boundary fences fence #2", "kind": "per-file", "mechanical": false}, {"i": 8, "ruleId": "band-no-skip", "severity": "🟠", "file": "src/commands/shared.ts", "lineStart": 4, "lineEnd": 6, "quote": "import { checkPostgresConnectivity } from \"../check/postgres-connectivity.js\";\nimport { checkS3Connectivity } from \"../check/s3-connectivity.js\";\nimport { checkSourceConnectivity } from \"../check/source-connectivity.js\";", "citation": "conv: §A — Boundary fences fence #2", "kind": "per-file", "mechanical": false}, {"i": 9, "ruleId": "band-downward-only", "severity": "🟠", "file": "src/types/run-summary.ts", "lineStart": 7, "lineEnd": 7, "quote": "import type { IngestStagingResult } from \"../staging/types.js\";", "citation": "conv: §A — Boundary fences fence #1", "kind": "per-file", "mechanical": false}, {"i": 10, "ruleId": "band-downward-only", "severity": "🟠", "file": "src/types/run-summary.ts", "lineStart": 4, "lineEnd": 4, "quote": "import type {\n  DiagnosticCode,\n  DiscoveryDiagnostic,\n  ReplayCandidate,\n} from \"../discovery/types.js\";", "citation": "conv: §A — Boundary fences fence #1", "kind": "per-file", "mechanical": false}, {"i": 11, "ruleId": "eslint-no-blanket-file-disable", "severity": "🟠", "file": "src/discovery/source-client.test.ts", "lineStart": 1, "lineEnd": 1, "quote": "/* eslint-disable max-lines -- Source-client transport tests keep direct and SSH behavior together. */", "citation": "std: §C — no blanket file-level eslint-disable", "kind": "mechanical", "mechanical": true}, {"i": 12, "ruleId": "typed-errors-only", "severity": "🟠", "file": "src/evidence/object-key.ts", "lineStart": 35, "lineEnd": 35, "quote": "    throw new Error(\"Evidence object-key prefix must not be empty\");", "citation": "std: §B — typed error hierarchy is mandatory; conv: Phase 2 sweep item 3", "kind": "per-file", "mechanical": false}, {"i": 13, "ruleId": "external-service-error-taxonomy", "severity": "🟠", "file": "src/checkpoint/s3-checkpoint-store.ts", "lineStart": 186, "lineEnd": 191, "quote": "  } catch (error) {\n    if (isNotFound(error)) {\n      return {};\n    }\n    throw error;\n  }", "citation": "std: correctness → External adapters — error taxonomy", "kind": "per-file", "mechanical": false}, {"i": 14, "ruleId": "swallowed-error", "severity": "🟠", "file": "src/discovery/html.ts", "lineStart": 59, "lineEnd": 61, "quote": "  } catch {\n    return undefined;\n  }", "citation": "std: correctness §AA — swallowed errors", "kind": "per-file", "mechanical": false}, {"i": 15, "ruleId": "graceful-shutdown", "severity": "🟠", "file": "src/commands/discover.ts", "lineStart": 153, "lineEnd": 232, "quote": "const runStoreRawDiscovery = async (\n  dependencies: Required<BuildCliDependencies>,\n  shouldStage: boolean,\n): Promise<void> => {", "citation": "std: correctness → Process lifecycle — graceful shutdown", "kind": "per-file", "mechanical": false}, {"i": 16, "ruleId": "no-unexplained-as-cast", "severity": "🟡", "file": "src/discovery/source-client.ts", "lineStart": 40, "lineEnd": 40, "quote": "const defaultExecFile = promisify(execFileCallback) as ExecFile;", "citation": "std: §B (TS code style) — no unexplained as casts", "kind": "per-file", "mechanical": false}, {"i": 17, "ruleId": "zod-bounded-fields", "severity": "🟡", "file": "src/config.ts", "lineStart": 100, "lineEnd": 104, "quote": "    sourceTimeoutMs: z.coerce\n      .number()\n      .int()\n      .positive()\n      .default(defaultSourceTimeoutMs),", "citation": "conv: §C — Bound every field; std: §D — unbounded field is a DoS vector", "kind": "per-file", "mechanical": false}, {"i": 18, "ruleId": "srp-function-length", "severity": "🟡", "file": "src/commands/check.ts", "lineStart": 29, "lineEnd": 89, "quote": "export const registerCheckCommand = (\n  program: Command,\n  dependencies: Required<BuildCliDependencies>,\n): void => {\n  program\n    .command(\"check\")\n    .description(\"Validate required configuration before running ingest work\")\n    .action(async () => {\n      try {\n        const config = dependencies.loadConfig();\n        const sourceClient = dependencies.createSourceClient(config);\n        // One S3 client + one pg pool per command, built once at the\n        // composition root and injected into the read-only probes\n        // ([std: correctness] External adapters one-client rule).\n        const s3Client = dependencies.createS3Client(config.s3);\n        const pool = dependencies.createPgPool(config.staging.databaseUrl);\n        const sourceConnectivity = await dependencies.checkSourceConnectivity({\n          sourceClient,\n          sourceUrl: new URL(config.sourceUrl),\n        });\n        const s3Connectivity = await dependencies.checkS3Connectivity({\n          bucket: config.s3.bucket,\n          sender: s3Client,\n        });\n        const stagingConnectivity = await runStagingCheck(dependencies, pool);\n        const checks = {\n          s3Connectivity,\n          sourceConnectivity,\n          stagingConnectivity,\n        };\n        const ok = connectivityOk(checks);\n\n        writeJson({\n          ok,\n          checks: {\n            config: { status: \"passed\" },\n            ...checks,\n          },\n          config: redactConfig(config),\n        });\n\n        if (!ok) {\n          process.exitCode = 2;\n        }\n      } catch (error) {\n        if (error instanceof ConfigValidationError) {\n          writeJson({\n            ok: false,\n            checks: {\n              config: { status: \"failed\" },\n            },\n            issues: error.issues,\n          });\n          process.exitCode = 2;\n          return;\n        }\n\n        throw error;\n      }\n    });\n};", "citation": "std: correctness → SOLID — SRP", "kind": "per-file", "mechanical": false}, {"i": 19, "ruleId": "srp-function-length", "severity": "🟡", "file": "src/discovery/source-client.ts", "lineStart": 180, "lineEnd": 221, "quote": "const runWithRetry = async <TResult>(\n  wiring: RetryWiring<TResult>,\n  options?: SourceFetchOptions,\n): Promise<TResult> => {\n  const attempts = options?.attempts ?? noRetryAttempts;\n  const callerSignal = options?.signal ?? new AbortController().signal;\n\n  let retryOptions: RetrySourceReadOptions<TResult> = {\n    attempts,\n    classify: wiring.classify,\n    phase: wiring.phase,\n    read: wiring.read,\n    signal: callerSignal,\n    url: wiring.url.toString(),\n  };\n\n  if (options?.page !== undefined) {\n    retryOptions = { ...retryOptions, page: options.page };\n  }\n\n  if (options?.onRetry !== undefined) {\n    retryOptions = { ...retryOptions, onRetry: options.onRetry };\n  }\n\n  if (wiring.retryAfterMs !== undefined) {\n    retryOptions = { ...retryOptions, retryAfterMs: wiring.retryAfterMs };\n  }\n\n  if (options?.sleep !== undefined) {\n    retryOptions = { ...retryOptions, sleep: options.sleep };\n  }\n\n  if (options?.random !== undefined) {\n    retryOptions = { ...retryOptions, random: options.random };\n  }\n\n  if (options?.now !== undefined) {\n    retryOptions = { ...retryOptions, now: options.now };\n  }\n\n  return withRetry(retryOptions);\n};", "citation": "std: correctness → SOLID — SRP", "kind": "per-file", "mechanical": false}, {"i": 20, "ruleId": "srp-function-length", "severity": "🟡", "file": "src/run/run-once.ts", "lineStart": 943, "lineEnd": 1010, "quote": "const assembleResult = async (\n  input: RunOnceInput,\n  context: AssembleResultInput,\n): Promise<RunOnceResult> => {\n  const sourceFailure = deriveSourceFailure(context.discoveryReport);\n  const discoveredLastPage = deriveDiscoveredLastPage(context);\n  const status = deriveRunStatus({\n    discoveredLastPage,\n    lastCompletedPage: context.lastCompletedPage,\n    ok: context.discoveryReport.ok,\n    reachedMaxPages: context.reachedMaxPages,\n    ...sourceFailureOption(sourceFailure),\n  });\n\n  if (status === \"complete\") {\n    await writeFinalCheckpoint(input, context, discoveredLastPage);\n  }\n\n  const summary = buildRunSummary({\n    candidateCount: context.discoveryReport.candidates.length,\n    discoveredLastPage,\n    discoveryReport: context.discoveryReport,\n    finishedAt: input.now().toISOString(),\n    lastCompletedPage: context.lastCompletedPage,\n    pageTimestampsMs: context.pageTimestampsMs,\n    rawStorage: context.rawStorage,\n    runId: input.runId,\n    staging: context.staging,\n    startedAt: context.startedAt,\n    status,\n    ...discoveredRangeOption(context.lastCompletedPage),\n    ...resumeInvocationOption(status),\n  });\n\n  // D-03/D-04: emit run_complete (info) when the run finished every discovered\n  // page, or run_partial (warn) for any non-complete status. The payload is\n  // identifiers-only; the message is static.\n  if (status === \"complete\") {\n    input.log?.info(\n      {\n        event: \"run_complete\",\n        runId: input.runId,\n        status,\n        counts: summary.counts,\n      },\n      \"run complete\",\n    );\n  } else {\n    input.log?.warn(\n      {\n        event: \"run_partial\",\n        runId: input.runId,\n        status,\n        counts: summary.counts,\n      },\n      \"run partial\",\n    );\n  }\n\n  // D-12/D-13: opt-in evidence writes (independent, log-and-continue).\n  // Each write is gated independently so both/either/neither may be active.\n  await writeEvidence(input, summary);\n\n  return {\n    exitCode: runExitCode(summary),\n    summary,\n  };\n};", "citation": "std: correctness → SOLID — SRP", "kind": "per-file", "mechanical": false}, {"i": 21, "ruleId": "srp-function-length", "severity": "🟡", "file": "src/discovery/html.ts", "lineStart": 79, "lineEnd": 129, "quote": "const parseReplayRow = (\n  rowHtml: string,\n  page: number,\n  sourceUrl: URL,\n): ReplayRowObservation => {\n  const cells = [\n    ...rowHtml.matchAll(/<td[^>]*>(?<cell>[\\s\\S]*?)<\\/td>/giu),\n  ].map((match) => getMatchGroup(match, \"cell\"));\n  const link =\n    /<a\\b[^>]*\\bhref=(?<quote>[\"'])(?<href>.*?)\\k<quote>[^>]*>(?<text>[\\s\\S]*?)<\\/a>/iu.exec(\n      cells[0] ?? \"\",\n    );\n  const href = link?.groups?.[\"href\"]?.trim();\n  const url = hrefToUrl(href, sourceUrl);\n  const externalId = /\\/replays\\/(?<id>[^/?#]+)/iu.exec(url ?? \"\")?.groups?.[\n    \"id\"\n  ];\n  const serverIdText = stripTags(cells[2] ?? \"\").trim();\n  const serverId = Number.parseInt(serverIdText, 10);\n  const source: MutableReplayRowSource = {};\n  const metadata: MutableReplayRowMetadata = {};\n\n  if (externalId !== undefined) {\n    source.externalId = externalId;\n  }\n\n  if (url !== undefined) {\n    source.url = url;\n  }\n\n  const missionText = stripTags(link?.groups?.[\"text\"] ?? \"\").trim();\n  const world = stripTags(cells[1] ?? \"\").trim();\n\n  if (missionText.length > 0) {\n    metadata.missionText = missionText;\n  }\n\n  if (world.length > 0) {\n    metadata.world = world;\n  }\n\n  if (!Number.isNaN(serverId)) {\n    metadata.serverId = serverId;\n  }\n\n  return {\n    metadata,\n    page,\n    source,\n  };\n};", "citation": "std: correctness → SOLID — SRP", "kind": "per-file", "mechanical": false}, {"i": 22, "ruleId": "type-over-interface", "severity": "🔵", "file": "src/contract-check/contract-check.test.ts", "lineStart": 172, "lineEnd": 172, "quote": "interface ListFetchFailureCase {", "citation": "std: §B (TS code style) — use type over interface", "kind": "mechanical", "mechanical": true}, {"i": 23, "ruleId": "import-order-grouped", "severity": "🔵", "file": "src/discovery/source-client.ts", "lineStart": 2, "lineEnd": 21, "quote": "import { AppError } from \"../errors/app-error.js\";\nimport { parseRetryAfter } from \"../source/backoff.js\";\nimport { classifyFailure } from \"../source/classify-failure.js\";\nimport { withRetry } from \"../source/retry.js\";\n\nimport type {\n  ClassifyInput,\n  FailureClassification,\n  FailureKind,\n} from \"../source/classify-failure.js\";\nimport type {\n  RetrySourceReadOptions,\n  SourceReadPhase,\n} from \"../source/retry.js\";\n\nimport type { SourceConfig } from \"../config.js\";\nimport type { SourceClient, SourceFetchOptions } from \"./types.js\";", "citation": "std: §B — keep imports grouped and alphabetized", "kind": "mechanical", "mechanical": true}, {"i": 24, "ruleId": "const-as-const-enum", "severity": "🟡", "file": "src/run/run-once.ts", "lineStart": 748, "lineEnd": 748, "quote": "    status: \"running\",", "citation": "std: §C — const object + derived union, never TS enum keyword", "kind": "mechanical", "mechanical": true}, {"i": 25, "ruleId": "type-over-interface", "severity": "🔵", "file": "src/checkpoint/s3-checkpoint-store.ts", "lineStart": 51, "lineEnd": 51, "quote": "interface S3CheckpointSenderOutput {", "citation": "std: §B (TS code style) — use type over interface", "kind": "mechanical", "mechanical": true}, {"i": 26, "ruleId": "const-as-const-enum", "severity": "🟡", "file": "src/contract-check/contract-check.ts", "lineStart": 123, "lineEnd": 123, "quote": "    return { message, ok: false, reason: \"contract_broken\", warnings };", "citation": "std: §C — const object + derived union, never TS enum keyword", "kind": "mechanical", "mechanical": true}]

const VERDICT = {
  type: 'object',
  required: ['real', 'confidence', 'reason'],
  properties: {
    real: { type: 'boolean', description: 'true ONLY if this is a genuine violation of the cited rule on live production code' },
    confidence: { type: 'string', enum: ['high', 'medium', 'low'] },
    reason: { type: 'string', description: 'one or two lines: what you found at the cited span and why it does or does not violate the rule' },
  },
}

phase('Verify')

const results = await parallel(
  SAMPLE.map((x) => () =>
    agent(
      `You ADVERSARIALLY verify ONE convention-audit finding against the source. Default SKEPTICAL — your job is to catch false positives. Run every command as \`git -C ${REPO} ...\` and read files by absolute path under \`${REPO}\`; read the rule's own skill under \`${REPO}/.claude/skills/\` if you need the exact rule wording.

The finding CLAIMS:
- rule: \`${x.ruleId}\` (${x.citation || 'no citation'})  severity ${x.severity}  kind ${x.kind}${x.mechanical ? ' (mechanical/pattern rule)' : ''}
- location: \`${x.file}\` lines ${x.lineStart}-${x.lineEnd}
- quoted span:
\`\`\`
${x.quote}
\`\`\`

Re-Read that span yourself. Set \`real\` = true ONLY if ALL hold: (1) the quoted code is actually there at/near those lines; (2) it is LIVE production code — not a comment, string, test/fixture, or dead branch; (3) it genuinely breaks the cited rule as that rule is actually written (do not invent a stricter rule). For a mechanical/pattern rule, (3) means the pattern truly matches. If any fails, \`real\` = false with the reason. Return the schema only.`,
      { schema: VERDICT, label: `check:${x.ruleId}:${x.lineStart}`, phase: 'Verify', model: 'haiku' }
    ).then((v) => ({ ...x, verdict: v }))
  )
)

const done = results.filter((r) => r && r.verdict)
const real = done.filter((r) => r.verdict.real)
const fp = done.filter((r) => !r.verdict.real)
const byStratum = {}
for (const r of done) {
  const key = r.mechanical ? 'mechanical' : r.severity
  byStratum[key] = byStratum[key] || { n: 0, real: 0 }
  byStratum[key].n++
  if (r.verdict.real) byStratum[key].real++
}
return {
  sampled: SAMPLE.length,
  verified: done.length,
  realCount: real.length,
  falsePositiveCount: fp.length,
  fpRate: done.length ? +(fp.length / done.length).toFixed(3) : null,
  byStratum,
  falsePositives: fp.map((r) => ({ ruleId: r.ruleId, file: r.file, line: r.lineStart, severity: r.severity, reason: r.verdict.reason })),
  lowConfidenceReals: real.filter((r) => r.verdict.confidence === 'low').map((r) => ({ ruleId: r.ruleId, file: r.file, line: r.lineStart, reason: r.verdict.reason })),
}
